import { css } from "styled-components";

export const animation = {
  scaleUp: css`
    @keyframes scaleUp {
      from {
        scale: 0.5;
      }
      to {
        scale: 1;
      }
    }
  `,
};
